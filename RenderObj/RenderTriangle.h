#pragma once

#ifndef __RENDERTRIANGLE_H__
#define __RENDERTRIANGLE_H__

// #include "RenderObj.h"
// #include <DirectXMath.h>

// class RenderTriangle : public RenderObj {
// public:
// 	RenderTriangle(std::string uuid, DirectX::XMFLOAT3* ps, DirectX::XMFLOAT4* cs);
// 	virtual ~RenderTriangle();

// 	bool init(ComPtr<ID3D11Device> dev);
// 	void render(ComPtr<ID3D11DeviceContext> devCon) const;

// 	void updatePos(DirectX::XMFLOAT3 *ps);
// 	void updateColor(DirectX::XMFLOAT4 *cs);

// private:
// 	bool initDx(ComPtr<ID3D11Device> dev);

// 	DirectX::XMFLOAT3 poses[3];
// 	DirectX::XMFLOAT4 colors[3];
// };

#endif
